<!-- pages/knowledge/detail/index.wxml -->
<view class="detail-container" wx:if="{{!isLoading}}">
  <!-- 顶部标题：英文单词 -->
  <view class="header-section">
    <text class="header-label">英文：</text>
    <text class="header-value">{{currentEn}}</text>
    <button class="read-btn" bindtap="readCurrentEn">🔊</button>
  </view>

  <!-- 合并的知识点列表 -->
  <view class="knowledge-list">
    <view class="knowledge-item" wx:for="{{mergedKnowledgeList}}" wx:key="_id">
      <!-- 词性+释义+出处 -->
      <view class="def-line">
        <text class="pos">{{item.pos}}</text>
        <text class="zh">{{item.zh}}</text>
        <text class="source">【出自：{{item.libraryName}}】</text>
      </view>

      <!-- 例句+朗读按钮 -->
      <view class="sentence-line" wx:if="{{item.example}}">
        <text class="sentence">{{item.example}}</text>
        <text class="read-icon" bindtap="readSentence" data-sentence="{{item.example}}">🔊</text>
      </view>
    </view>
  </view>
</view>

<!-- 加载中提示 -->
<view class="loading-tip" wx:else>
  <text>加载详情中...</text>
</view>